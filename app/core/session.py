"""Async database engine and session lifecycle.

Empty for now. Will create the SQLAlchemy 2.0 async engine and an
`async_sessionmaker`, consumed by the repositories through `get_db`.
"""

# TODO: async engine, async_sessionmaker.
