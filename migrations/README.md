# Migrations

Alembic migrations live here, next to the models. Configured in Phase 0.

- `versions/` holds the individual migration scripts.
- `env.py` and `script.py.mako` are generated by `alembic init`.

This README is a placeholder so the folder exists in the skeleton.
